"""Training-stage validation and reusable objectives.

The lightweight repository does not download model weights automatically. The
entry points validate configs and record reproducibility metadata before a user
connects them to a pinned Qwen training environment.
"""

